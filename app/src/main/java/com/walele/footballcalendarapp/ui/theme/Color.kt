import androidx.compose.ui.graphics.Color

// Versione Light (già definita sopra)
val BackgroundColor = Color(0xFFFFFFFF) // Bianco
val PrimaryTextColor = Color(0xFF121212) // Quasi nero
val SecondaryTextColor = Color(0xFF757575) // Grigio

val OrangeAccent = Color(0xFFFF6B00) // Arancione intenso
val GreenAccent = Color(0xFF00A86B) // Verde campo

val BlueAccent = Color(0xFF0096FF) // Azzurro
val YellowAccent = Color(0xFFFFD600) // Giallo

// Versione Dark
val BackgroundDark = Color(0xFF000000) // Background scuro
val PrimaryTextDark = Color(0xFFFFFFFF) // Testo chiaro per il tema scuro
val SecondaryTextDark = Color(0xFFB0B0B0) // Grigio chiaro per testo secondario

val OrangeAccentDark = Color(0xFFFF6B00) // Arancione intenso (resta invariato)
val GreenAccentDark = Color(0xFF00A86B) // Verde campo (resta invariato)

val BlueAccentDark = Color(0xFF0096FF) // Azzurro (resta invariato)
val YellowAccentDark = Color(0xFFFFD600) // Giallo (resta invariato)